#include <stdio.h>
int main()
{
    printf("Even numbers from 1 to 50:\n");
    for(int a =0;a<=50;a++)
    while (a <= 50) 
    {
        if (a % 2 == 0)
        {
            printf("%d \n", a);
        }
        a++;
    }

    return 0;
}